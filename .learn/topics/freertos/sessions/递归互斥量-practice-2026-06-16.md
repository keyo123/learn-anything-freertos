# Practice Session - 2026-06-16

## Concept Practiced
- Concept: 递归互斥量
- Difficulty: Intermediate
- Exercise Name: 递归锁嵌套获取与超时观察

## User's Submitted Code
在 [demo_rec_mutex.c](file:///c:/Users/24212/Desktop/za/study/learn-anything/01-FreeRTOS-LED/User/demo_rec_mutex.c) 中：
```c
static void print_nested_log(int depth) {
  if (depth <= 0)
    return;

  /* 使用递归拿锁 API 获取递归互斥量，设定超时为 portMAX_DELAY */
  if (xSemaphoreTakeRecursive(xRecMutex, portMAX_DELAY) == pdTRUE) {
    printfSafe("[Task] Entered depth %d. RecMutex taken.\r\n", depth);

    /* 递归调用自身 */
    print_nested_log(depth - 1);

    printfSafe("[Task] Leaving depth %d. Giving RecMutex.\r\n", depth);

    /* 释放递归互斥量：只在深度大于 1 时释放锁（模拟不完全释放实验） */
    if (depth > 1) {
      xSemaphoreGiveRecursive(xRecMutex);
    }
  }
}

static void vTaskObserver(void *pvParameters) {
  (void)pvParameters;
  vTaskDelay(pdMS_TO_TICKS(1000)); // 让 Demo 任务先跑完

  printfSafe("[Observer] Trying to take lock...\r\n");

  /* 尝试拿锁 */
  if (xSemaphoreTakeRecursive(xRecMutex, pdMS_TO_TICKS(1000)) == pdTRUE) {
    printfSafe("[Observer] Successfully took lock!\r\n");
    xSemaphoreGiveRecursive(xRecMutex);
  } else {
    printfSafe(
        "[Observer] TIMEOUT! Cannot take lock (is it fully released?)\r\n");
  }

  vTaskDelete(NULL);
}
```

## AI Feedback

1. **🎉 完美的实验验证**：
   * 在正常成对释放（Take 3 次，Give 3 次）时，嵌套函数顺利递归返回，观察者任务 `Observer` 随后成功获取锁。
   * 在通过 `if (depth > 1)` 故意引入“少释放一次”（只 Give 了 2 次）的逻辑错误后，观察者任务在 1000ms 超时限制内无法获得该锁，准确地输出了 `TIMEOUT! Cannot take lock` 现象。

2. **💡 深刻理解嵌套计数（Nesting Count）的核心**：
   * 递归互斥量与普通互斥量的最大区别在于它具有“重入所有权”和“嵌套计数器”。
   * 必须实现完全相等的 Give/Take 次数，嵌套计数器降到 0 后，锁才会真正归还内核，对外部就绪任务重新可见。

3. **📊 调度时间差修复**：
   * 在实验中通过将 `Observer` 延时从 100ms 调整为 1000ms，避开了因延迟比 `RecDemo`（500ms）短而提前执行的现象，保证了“持有锁”与“获取锁”的先后时序。

## Assessment
- Understanding: Excellent
- Status update: in_progress → mastered
- confidence: 0.3 → 0.8
